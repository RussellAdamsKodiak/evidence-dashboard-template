# Elasticsearch connection
ES_URL=https://your-cluster.es.io:9200
ES_API_KEY=your_base64_api_key_here

# Index and field names (defaults shown — override if your schema differs)
ES_INDEX=logs-*
ES_TIMESTAMP_FIELD=@timestamp
ES_LEVEL_FIELD=log.level
